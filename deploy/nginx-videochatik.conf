# Initial nginx site for videochatik.online. Certbot can extend this file with SSL.
# The chatruletka app container must be reachable on this host at 127.0.0.1:3005.
server {
    listen 80;
    listen [::]:80;
    server_name videochatik.online www.videochatik.online;

    location / {
        proxy_pass http://127.0.0.1:3005;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 75s;
    }
}
