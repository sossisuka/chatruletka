# Проверка приложения

Дата: 11 сентября 2026. Дополнительная проверка SEO и Google Search Console для `videochatik.online`.

## Результаты SEO

- `npm run typecheck`: успешно.
- `npm run lint`: без ошибок.
- `npm run build`: успешно. Next.js собрал статические `/robots.txt`, `/sitemap.xml` и динамический route `/[verification]` для Google verification-файла.
- Локальный production-сервер на `http://127.0.0.1:3122` с `GOOGLE_SITE_VERIFICATION_FILE=google123abc.html`: `/robots.txt` HTTP 200, `/sitemap.xml` HTTP 200, `/google123abc.html` HTTP 200.
- Неизвестные `/google-wrong.html` и `/random-page`: HTTP 404.
- HTML главной страницы содержит title, description, canonical `https://videochatik.online`, `robots index, follow`, `googlebot index, follow`, Open Graph и Twitter-card metadata. `noindex` больше не отдаётся.
- Локальный production-сервер на `http://127.0.0.1:3125`: `public/googleb5037eb4a814b1fc.html` отдаётся как `/googleb5037eb4a814b1fc.html` с HTTP 200, `/manifest.webmanifest` HTTP 200, PWA-иконки 180/192/512 PNG доступны, HTML содержит `link rel="manifest"` и `link rel="apple-touch-icon"`.
- Тексты SEO и главной страницы содержат фразы `чат-рулетка` и `для русских пользователей`. Синий фон `.remote-panel` сверён с первым коммитом `0f8c820` и возвращён к исходному градиенту `#283e5a -> #1b2b41 -> #172436`.

Дата: 11 сентября 2026. Дополнительная проверка после подключения серверного определения страны через 2ip и смены домена на `videochatik.online`.

## Результаты 11 сентября 2026

- DNS: `videochatik.online` и `www.videochatik.online` указывают на `185.128.200.106`.
- `http://videochatik.online`: отвечает nginx 200 со стандартной страницей.
- `https://videochatik.online`: сертификат не проходит проверку имени, поэтому на сервере нужно включить nginx-конфиг `deploy/nginx-videochatik.conf` и выпустить сертификат Certbot для `videochatik.online` и `www.videochatik.online`.
- TypeScript `npm run typecheck`: успешно.
- ESLint `npm run lint`: без ошибок.
- Серверные тесты `npm test`: 24/24, включая определение IP посетителя за nginx, интеграцию 2ip с мок-ответами официального формата, защиту от подмены страны клиентом, лимиты по IP посетителя и подбор по стране в обе стороны.
- Production-сборка `npm run build`: успешно.
- `git diff --check`: успешно, только предупреждения Git о будущей замене LF на CRLF в рабочей копии Windows.
- Docker daemon в финальной проверке был недоступен в Docker Desktop (`dockerDesktopLinuxEngine` не запущен), поэтому повторный Docker-run после доменной правки не выполнялся. До правки домена Docker-образ приложения собирался, production-контейнер запускался на `127.0.0.1:3110`, а браузерные тесты проходили на этом образе; доменная правка затрагивает только README и nginx-шаблон.

Дата: 10 сентября 2026. Среда: Windows, Node.js 24.16.0, Next.js 16.3.4, Chromium; Docker Engine 29.7.2, Linux-контейнер с Node.js 22.23.2.

## Результаты

- Production-сборка `npm run build`: успешно.
- TypeScript `npm run typecheck`: успешно.
- ESLint `npm run lint`: без ошибок и предупреждений.
- Серверные интеграционные тесты `npm test`: 12/12, включая проверку публичного Origin.
- Браузерные тесты `npm run test:e2e`: 9/9 на production-сборке в Docker, 23.1 секунды. Для сценариев подбора использован отдельный контейнер того же образа на `127.0.0.1:3105` без других посетителей; проверка внешнего HTTP выполнялась через `chatruletka.test:3005` на основном контейнере.
- `docker compose --profile public --profile turn config --quiet`: успешно.
- `docker compose up -d --build --wait app`: Linux-образ собран, контейнер `app` запущен со статусом `healthy`, работает от пользователя `node` без перезапусков.
- `/assets/icons/revolvercylinde.svg`: HTTP 200 из контейнера. Каталог `public` включён в финальный Docker-образ; новая иконка используется в шапке, поиске и подвале, старые Aperture/Shuffle отсутствуют.
- `/api/health`: HTTP 200, реальные счётчики активных соединений.
- agent-browser: страница загружается, основные элементы доступны, ошибок браузера не обнаружено.
- Desktop 1440×1100 и mobile 390×844: визуально проверены; горизонтального переполнения на mobile нет.
- Публичный адрес `http://185.128.200.106:3005`: Socket.IO подключается, показывается реальный онлайн и предупреждение о необходимости HTTPS для камеры.
- Dev-сервер: HMR и Socket.IO одновременно поддерживают WebSocket с `Origin: http://185.128.200.106:3006` и соответствующим Host. Соединения оставались открыты после 2.5 секунд.
- Анимации индикатора и диалога активны по умолчанию и отключаются при `prefers-reduced-motion: reduce`.
- Флаги всех восьми стран загружаются из FlagCDN в формате SVG. Изображения используются в списках, выбранных значениях и подписи собеседника; для общего поиска и «Другой страны» используется глобус.
- Выбор страны работает мышью и клавиатурой (стрелки, Home/End, Enter, Escape), сохраняет фокус и не закрывает настройки при первом Escape. На экране 390×844 список открывается вверх и полностью помещается в экран, горизонтального переполнения нет.
- Внешние рамки карточек, фильтров, кнопок и диалогов убраны. Все три иконки барабана вращаются с замедлением и паузой; основной цикл длится 9 секунд, при поиске — 3.8 секунды. В браузере измерено снижение угла за 100 мс с 60.54° в начале до 1.30° в конце и 0° во время паузы.

## Проверенные сценарии

Сервер: ожидание первого посетителя, уникальная пара, двусторонние фильтры страны, изоляция сообщений и SDP, отклонение поддельных и устаревших сессий, следующий собеседник, отсутствие немедленного повторного подбора, очистка очереди после Stop/disconnect, временная блокировка, лимиты сообщений, проверка Origin/Referer, подписанные временные TURN-ключи.

Браузер: независимые контексты устанавливают WebRTC и получают воспроизводимое видео ненулевого размера и живые аудиотреки. Проверены двусторонние сообщения и безопасный вывод HTML как текста, микрофон/камера, освобождение media tracks по Stop, третий участник и Next, очистка черновика после Next, фильтр страны, отказ в разрешениях, Stop во время ожидания разрешений и мобильные диалоги.

Устройства в автоматических проверках синтетические. Очередь, Socket.IO, WebRTC и обмен медиаданными не подменены. Только тесты ошибок доступа и позднего разрешения намеренно переопределяют getUserMedia.

## Артефакты

- `artifacts/desktop-production.png` — основной экран production.
- `artifacts/mobile.png` — мобильный экран production.
- `artifacts/connected.png` — соединение двух тестовых браузеров.
- `artifacts/reference/` — исходные снимки страницы из архива.
- `artifacts/production.log` / `production-error.log` — локальный сервер.
- `artifacts/external-http-fixed.png` — внешний HTTP-адрес после исправления подключения.
- `artifacts/verify-dev-hmr.mjs` — выполненная проверка совместной работы WebSocket чата и HMR.
- `artifacts/countries-desktop.png`, `artifacts/countries-mobile.png` — списки стран с флагами FlagCDN.
- `artifacts/interface-mobile.png` — интерфейс без внешних рамок на мобильном экране.

## Что не проверено

Не запускались дополнительные контейнеры `caddy` и `turn`: для профилей `public` и `turn` проверен синтаксис Compose. HTTPS-сертификат для внешнего IP не выпущен. На порту 80 внешнего адреса отвечает nginx, сертификат на 443 не проходит проверку имени IP. Настройка nginx требует доступа администратора к внешнему серверу; файлы подготовлены в `deploy/`. Проверки не подтверждают работу внешнего TURN или передачу с физических камер. На внешнем HTTP браузер не предоставляет getUserMedia — этот адрес пока не подходит для видеозвонков.

Основной Docker-контейнер оставлен работающим на http://127.0.0.1:3005. Временный контейнер для тестов остановлен и удалён после проверки.
